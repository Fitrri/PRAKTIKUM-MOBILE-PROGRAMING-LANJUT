name: utd_advanced_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^17.1.0
  get_it: ^9.2.1
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8
  dio: ^5.9.2
  logger: ^2.7.0
  web_socket_channel: ^3.0.3
  # Menggunakan versi stabil yang aman untuk data lama Anda
  isar: 3.1.0+1
  isar_flutter_libs: 3.1.0+1
  path_provider: ^2.1.5
  workmanager: ^0.9.0+3
  shared_preferences: ^2.5.5
  intl: ^0.20.2
  lottie: ^3.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Paket pengujian integrasi Modul 13 terpasang langsung
  integration_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  isar_generator: 3.1.0+1
  mocktail: ^1.0.5

flutter:
  uses-material-design: true